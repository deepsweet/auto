import { TOptions } from '../src'

export const options: TOptions = {
  prefixes: {
    patch: ['✔️'],
    minor: ['➕'],
    major: ['💥', '🚨'],
    publish: ['📦'],
    dependencies: ['♻️']
  },
  namespace: 'ns'
}
